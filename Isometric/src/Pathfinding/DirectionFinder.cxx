/* TODO
#include "Serialization/Serializeable.hxx"
#include "Tiles/TileBasics.hxx"
#include "Pathfinding/DirectionFinder.hxx"

namespace Core4
{
    //------------------------------------------------------------------------------------------------------------
    IsoDirection DirectionFinder::getDirection(const Point & from, const Point & to)
    {
        for (int i = 0; i < 8; i++)
        {
   //         const Point & pt = TileBasics::getNeighbourCell(from, static_cast<IsoDirection>(i));
     //       if (pt == to) return static_cast<IsoDirection>(i);
        }
        return UnknownDirection;
    }
} // namespace Core4
*/